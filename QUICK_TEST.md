# 🧪 Быстрое тестирование Cellular Life

## Локальное тестирование (РЕКОМЕНДУЕТСЯ)

### Запуск сервера
```bash
cd c:\Users\Дмитрий\.gemini\antigravity\playground\cellular-life
python -m http.server 8080
```

### Открыть в браузере
```
http://localhost:8080/index.html
```

## Online тестирование

### GitHub Pages (живой деплой)
```
https://dmitriykontekstpro-lang.github.io/cellular-life-simulation/
```

## ✅ Что проверить

### 1. Визуальная проверка
- [ ] Синяя **ветвистая река** видна (должна быть более разветвленная чем раньше)
- [ ] Зеленые **растения** появляются рядом с водой
- [ ] Растения **растут вверх**
- [ ] Видно **ветвление** растений

### 2. Жизненный цикл
- [ ] Запустите симуляцию (кнопка "Старт")
- [ ] Подождите 30-60 секунд
- [ ] Откройте **консоль браузера** (F12)
- [ ] Найдите сообщения типа:
   ```
   Plant X reached max size Y, generating seeds...
   Generated N seeds from plant X
   ```

### 3. Семена
- [ ] **Ярко-желтые точки** должны появиться на верхушках растений
- [ ] Счетчик "Семена" должен быть > 0

### 4. Вода
- [ ] Вода НЕ блокирует свет (растения должны расти даже над водой)
- [ ] Радиус воды увеличен в 10 раз (должна покрывать большую площадь)

## 🐛 Отладка в консоли

Откройте консоль браузера (F12) и выполните:

```javascript
// Получить статистику
window.simulationEngine.getStats()

// Проверить конфигурацию
window.simulationEngine.config

// Принудительно создать растение
window.simulationEngine.plantManager.createRandomPlant()

// Остановить симуляцию
window.simulationEngine.pause()

// Запустить симуляцию
window.simulationEngine.start()

// Сбросить симуляцию
window.simulationEngine.reset()
```

## 📊 Ожидаемые результаты

После 60 секунд работы:
- **Растения**: 5-10 (некоторые умирают, некоторые достигают max size)
- **Семена**: 5-15 (ярко-желтые точки)
- **Вода**: ~1400 клеток
- **Тики**: 60,000+ (на скорости 5x)

## 🎯 Критерии успеха

✅ Река более ветвистая  
✅ Растения спавнятся у воды  
✅ Растения растут до максимального размера  
✅ Появляются ЖЕЛТЫЕ семена (не светло-зеленые!)  
✅ В консоли видны сообщения о генерации семян  
✅ Вода не блокирует свет
